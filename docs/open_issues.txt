

TODO


issue 
    seqtk sample tool input
    4_0=4.0

issue 
    collapse_dataset2 tool input
    if_nr==1_{print="{if (NR==1) {print}}"
    cooked

issue
    blank value for default attribute (bowtie2_wrapper)
    ToolInput(
		'rg',
		Int(optional=True),
		prefix='--rg',
		position=1,
		default=,
		doc="None",
	),
solution?
    default can't be blank string?
    in this case gets assigned None


issue   
    --discard-trimmed not appearing (cutadapt)
    probably due to recent ArgumentCommandAnnotator changes

issue
    picard_MarkDuplicates
    input_#="#",  
solution
    this was actually a broken tool.
    maybe get the most recent version of the tool when getting a repo path? 

