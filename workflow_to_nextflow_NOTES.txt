



.ga format is json containing:
    - inputs outputs
    - per step:
        - toolname (tool_id)
        - tool_state (the parameter settings)
        - IO


to parse to nextflow:
    - create steps, inp out etc
    - for each tool:
        - 
        - run the galaxy cheetah conversion using supplied tool_state
        - 

