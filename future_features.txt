

TODO:


- #set directives
- resolve split variables - will have same prefix
- remove duplicate params (bwa-mem)
- bug: cast to float possible when all select options are ''
- positional arguments?
- global macros, tokens and set directives
- resolve the janis input name to just last item in gx_var (ie adv.specificity -> specificity). if two params then have same janis name, add the prev item in the path eg adv.specificity -> advSpecificity. keep going until unambiguous. 



test output checking:
https://github.com/galaxyproject/galaxy/tree/dev/lib/galaxy/tool_util/verify/asserts




sort out if params are optional!

params:
    <repeat> tag 
    <options> tag 

param types TODO:
    datacollection elem 
    data_column elem 
    hidden elem 

link galaxy variables to temp variables created by cheetah function calls

tests extracted and automatically run


